Orika is a Java Bean mapping framework that recursively copies data from one object to another.

It is common that we need to convert objects from one instance into another to accommodate different APIs.

Orika uses byte code generation to create fast mappers with minimal overhead,
